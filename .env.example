# Wine Cellar App - Environment Variables

# Supabase Auth (required for multi-user)
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=your-supabase-anon-key

# PostgreSQL (Supabase) connection string for production
# Example: postgres://user:pass@db.<project>.supabase.co:5432/postgres
DATABASE_URL=postgres://user:pass@db.project.supabase.co:5432/postgres

# Anthropic API (required for AI features)
ANTHROPIC_API_KEY=sk-ant-api03-xxxxx

# OpenAI Reviewer Feature Flags
# Set to 'true' to enable GPT-5.2 review of zone reconfiguration plans
OPENAI_REVIEW_ZONE_RECONFIG=false

# Google Programmable Search (optional, for rating search)
# Get API key from: https://console.cloud.google.com/apis/credentials
# Get Engine ID from: https://programmablesearchengine.google.com/controlpanel/all
GOOGLE_SEARCH_API_KEY=AIzaSyXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX
GOOGLE_SEARCH_ENGINE_ID=a1b2c3d4e5f6g7h8i

# Bright Data API (optional, for enhanced search and scraping)
# Get API key from: https://brightdata.com/ -> Settings -> API tokens
# Single API key used for both SERP and Web Unlocker zones
# BRIGHTDATA_API_KEY=your-brightdata-api-key

# SERP Zone (optional) - structured Google search results
# Create zone at: https://brightdata.com/ -> Proxies & Scraping -> SERP API
# BRIGHTDATA_SERP_ZONE=your-serp-zone-name

# Web Unlocker Zone (recommended) - for fetching blocked pages (Vivino, CellarTracker)
# Create zone at: https://brightdata.com/ -> Proxies & Scraping -> Web Unlocker
# Enables: full page content, tasting notes extraction, accurate ratings
# BRIGHTDATA_WEB_ZONE=your-web-unlocker-zone-name

# Credential Encryption Key (recommended for production)
# Used to encrypt Vivino/Decanter login credentials stored in database.
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
# If not set, the app auto-generates a key and saves to .credential_key file (development only).
# WARNING: Changing this key will invalidate all stored credentials!
# CREDENTIAL_ENCRYPTION_KEY=your-base64-encoded-32-byte-key

# PDF OCR Method for award extraction (optional)
# Options:
#   - 'local': Use RolmOCR (requires Python + dependencies installed)
#   - 'claude': Use Claude API's native PDF support (more reliable, uses API credits)
#   - 'auto': Try local first, fall back to Claude if not available (default)
# PDF_OCR_METHOD=local
