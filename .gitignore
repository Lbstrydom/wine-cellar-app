node_modules/

# Legacy SQLite database files (PostgreSQL/Supabase is production)
data/cellar.db
data/cellar.db-wal
data/cellar.db-shm
data/awards.db-wal
data/awards.db-shm
# Ignore stray Windows artifact file
nul
.DS_Store
*.log
# Claude Code local settings (private)
.claude/settings.local.json

# MCP config (contains API keys)
.mcp.json

# Track skills (shared)
.env
.credential_key

# Benchmark results (generated by CI)
tests/benchmark/results/

# Extension build output
dist/
