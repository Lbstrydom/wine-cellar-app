node_modules/

# Personal cellar database (private data)
data/cellar.db
data/cellar.db-wal
data/cellar.db-shm

# Awards database is tracked (shared public data)
# Do NOT add data/awards.db to gitignore

# SQLite WAL files for awards db
data/awards.db-wal
data/awards.db-shm
# Ignore stray Windows artifact file
nul
.DS_Store
*.log
# Claude Code local settings (private)
.claude/settings.local.json

# MCP config (contains API keys)
.mcp.json

# Track skills (shared)
.env
.credential_key

# Benchmark results (generated by CI)
tests/benchmark/results/
